---

name: Generate Manual

on:
  push:
    paths:
      - 'docs/src/manual.md'
  workflow_dispatch:

jobs:
  gen_man:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - id: generate
        run: |
          sudo apt-get update
          sudo apt-get install pandoc
          ./man/gen-man
      - name: push updated manual
        run: |
          git config --global user.name denv-docs-bot
          git config --global user.email denv-docs-bot@users.noreply.github.com
          git switch -c man-update
          git add man
          git commit -m "Auto Man Page Update" || exit 0
          git push -fu origin man-update
          gh pr create -B main -H man-update --title "Auto Man Page Update"
        env:
          GH_TOKEN: ${{ github.token }}
